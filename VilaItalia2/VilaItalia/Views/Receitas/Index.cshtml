@model IEnumerable<VilaItalia.Models.Receita>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PrecoSugerido)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PrecoFixo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PrecoSugerido)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PrecoFixo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ReceitaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ReceitaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ReceitaId })
        </td>
    </tr>
}

</table>
